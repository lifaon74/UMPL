
begin loop value is : <%BEGIN= $core.loop %>

<%+2
	var str_level_2 = 'some text in string executed at loop ' + $core.loop;
%>
<%
	
	$buffer.write(
		'<%= str_level_2 %>' + ', and writed at loop ' + $core.loop
	);
	
	var str = "<%#= 'string with escaped char' +  ' writed at loop ' + $core.loop #%>";
%>

<%= str %>

end of file